on:
  push:
    branches:
      - master
  pull_request:

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: docker build .
  test-usage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./
        with:
          input_path: /
          output_dir: generated
          build_html: true
          images_dir: images
      - uses: actions/upload-artifact@v5
        with:
          name: readme
          path: generated
